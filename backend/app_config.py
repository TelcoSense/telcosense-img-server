from pathlib import Path

IMG_DIRS = {
    "raincz": Path("../telcorain/outputs_web").resolve(),
    "rainsum": Path("../telcorain/outputs_sum").resolve(),
    "tempcz": Path("../telcotemp/outputs_cml").resolve(),
    "tempchmi": Path("../telcotemp/outputs_meteo").resolve(),
}
